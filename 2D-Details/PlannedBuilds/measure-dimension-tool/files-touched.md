- **js/07-globals.js** — Add feature-prefixed globals: measureP1, measureDimInput, measureDimActive, measureAwaitId, dimEditor (after the platePts/plateDim* block ~line 52).
- **js/96-v25-dimension.js** — NEW band-9 file: drawDim2_2D renderer (dashed extension lines, paper-mm offset via _nbZoom, terminators, upside-down text flip, textOverride) + the dimOpenEditor/_dimCloseEditor/_dimPositionEditor/_dimEditorTick/_dimOutsideClick inline-editor lifecycle copied from noteBox.
- **index.html** — Add <script src="js/96-v25-dimension.js"></script> in the band-9 group (after other 2D-mode scripts, before 73-init.js).
- **js/69-v25-dispatch.js** — v25DrawEnt (line 8): dispatch dim2 to drawDim2_2D. v25TryHandleClick (~line 581, near v25-leader): new two-click v25-measure placement branch. v25SetTool (line 143): reset the measure* globals. v25ActiveTileId (~825): map v25-measure to its tile id.
- **js/38-crosshair.js** — drawClickPreview (~line 347): add v25-measure branch — crosshair before 1st click; ghost dim2 (reusing drawDim2_2D) + live mm/typed-buffer pill after 1st click.
- **js/71-v25-selection.js** — Add dim2 branches to v25EntBounds (~109), v25EntHandles (~359: p1/p2/off grips), v25HitHandle (~921), v25Move (before generic tail ~1296: p1/p2/off/body), v25UpdateInspector (~1372: offset/text-height/terminator/precision/units/override fields); optional precise v25HitTestStack branch (~231).
- **js/42-keyboard.js** — Add 2D-mode 'm' -> v25SetTool('v25-measure') above the mirror binding (line 29). Add a digit/letter capture block (modelled on lines 166-202) gated on tool==='v25-measure' && measureAwaitId: digits set length, letters set textOverride, Enter commits. Reset measure* globals in the Escape branch (~235).
- **js/39-events.js** — First dblclick listener (line 1281): after nbOpenEditorAt (~1301) and before the generic Settings fallback (~1336), route dim2 hits to dimOpenEditor.
- **js/74-v26-bb-rail.js** — getDrawTabDef 'Measure' section (line 323): add a 'Measure' tile (id v25-measure, icon icon-dim-aligned) routing to v25SetTool('v25-measure'), gated sheetMode==='2d'. Leave legacy a-aligned/a-dimH tiles as-is.
- **js/72-v25-options-bar.js** — v25UpdateOptionsBar (line 20): add an 'else if (tool==="v25-measure")' branch with Text height / Terminator / Precision / Units / Offset controls writing to v25Last. Optionally extend the v25Add monkey-patch (~299) to seed dim2 props from v25Last.
- **js/45-dxf-export.js** — _dxfEmit2DEntity (line 314): add an 'else if (ent.type==="dim2")' branch mirroring the legacy dim branch (line 363) — emit dim line + label (textOverride ?? rounded length) on layer S-DIM via _dxfBlockPlace/_dxfLine/_dxfText.
- **CHANGELOG.md** — Add one line: new 2D-mode Bluebeam-style Measure/dimension tool (key 'm'): two-click placement with ortho-default, type-digits-to-set-length / letters-for-label, drag dim line to re-offset, double-click to edit.
